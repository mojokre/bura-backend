[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
# Prod installs skip devDependencies; tsc still needs @types/* at build time.
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
